<section id="inicio" class="hero">
  <div class="container">
    <div class="hero-content fade-in-up">
      <h1>Alicia Susana Arias</h1>
      <p class="subtitle">Escritora de Literatura Contemporánea</p>
      <div class="hero-divider"></div>
      <p class="hero-quote">
        "Las palabras son ventanas al alma. A través de ellas, podemos tocar corazones, despertar conciencias y crear
        mundos donde la esperanza siempre encuentra su lugar."
      </p>
      <a href="#biografia" class="cta-button">Conocé mi historia</a>
    </div>
  </div>
</section>

<style>
  /* Hero Section */
  .hero {
    background: linear-gradient(135deg, #f8f6f2 0%, #e8e2d8 100%);
    padding: 150px 0 100px;
    text-align: center;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-content h1 {
    font-size: 4rem;
    margin-bottom: 20px;
    color: #5b391d;
    font-weight: 300;
  }

  .hero-content .subtitle {
    font-size: 1.5rem;
    color: #a0845c;
    margin-bottom: 30px;
    font-style: italic;
  }

  .hero-quote {
    font-size: 1.2rem;
    color: #6d5a47;
    max-width: 800px;
    margin: 0 auto 40px;
    font-style: italic;
    line-height: 1.8;
  }

  .hero-divider {
    width: 100px;
    height: 3px;
    background: #f7e2b3;
    margin: 30px auto;
  }

  .cta-button {
    display: inline-block;
    background-color: #5b391d;
    color: white;
    padding: 15px 35px;
    text-decoration: none;
    border-radius: 10px;
    font-size: 1.1rem;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(139, 69, 19, 0.3);
  }

  .cta-button:hover {
    background-color: #6d3d0f;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(139, 69, 19, 0.4);
  }
</style>
